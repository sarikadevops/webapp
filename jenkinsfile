pipeline {
agent{
  node{
      label"built-in"
      }
     }
     stages{
            stage('mvn'){
                  steps{
                        sh 'mvn clean package'
                       }
                          }
             stage('deploy'){
              steps{
                 sh 'docker build -t mytomcat .'
                 sh 'docker run -it -d -p 4000:8080 mytomcat'
                 }
                 }
          }
     }
